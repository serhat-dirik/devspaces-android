# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Flutter / Dart build artifacts (the app is scaffolded with `flutter create`)
app/build/
app/.dart_tool/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/.packages
**/*.apk
**/*.aab
**/*.ipa

# Emulator / Mac-pool lease state
.mac/
*.log

# Local environment
.env
